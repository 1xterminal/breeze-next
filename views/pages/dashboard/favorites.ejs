<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate">
  <meta http-equiv="Pragma" content="no-cache">
  <meta http-equiv="Expires" content="0">
  <title><%= typeof title !== 'undefined' ? title : 'Breeze Weather Favorites' %></title>
  <link href="/css/output.css" rel="stylesheet">
</head>
<body class="min-h-screen gradient-main bg-[length:200%] animate-[gradient-animation_40s_ease_infinite] bg-fixed">
    <%- include('../../components/navbar') %>

<div class="container mx-auto px-4 sm:px-6 lg:px-8 py-6">
  <div class="glass-card p-6">
    <div class="flex justify-between items-center mb-6">
      <h1 class="text-2xl font-bold text-white">Favorite Locations</h1>
      <a href="/dashboard" class="glass-button">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 inline mr-1" viewBox="0 0 20 20" fill="currentColor">
          <path fill-rule="evenodd" d="M9.707 16.707a1 1 0 01-1.414 0l-6-6a1 1 0 010-1.414l6-6a1 1 0 011.414 1.414L5.414 9H17a1 1 0 110 2H5.414l4.293 4.293a1 1 0 010 1.414z" clip-rule="evenodd" />
        </svg>
        Back to Dashboard
      </a>
    </div>

    <% if (typeof error !== 'undefined' && error) { %>
      <div class="glass-card-sm bg-red-400/30 text-white p-4 rounded-lg mb-6">
        <%= error %>
      </div>
    <% } %>

    <% if (favorites && favorites.length > 0) { %>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
        <% favorites.forEach(favorite => { %>
          <div class="glass-card-sm relative group">
            <form action="/favorites/remove" method="POST" class="absolute top-4 right-4 opacity-0 group-hover:opacity-100 transition-opacity">
              <input type="hidden" name="favoriteId" value="<%= favorite._id %>">
              <button type="submit" class="text-white/50 hover:text-white/80 p-1">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" viewBox="0 0 20 20" fill="currentColor">
                  <path fill-rule="evenodd" d="M4.293 4.293a1 1 0 011.414 0L10 8.586l4.293-4.293a1 1 0 111.414 1.414L11.414 10l4.293 4.293a1 1 0 01-1.414 1.414L10 11.414l-4.293 4.293a1 1 0 01-1.414-1.414L8.586 10 4.293 5.707a1 1 0 010-1.414z" clip-rule="evenodd" />
                </svg>
              </button>
            </form>

            <a href="/dashboard/search?city=<%= encodeURIComponent(favorite.name) %>" class="block">
              <div class="flex items-start mb-4">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 mr-2 text-white/70 flex-shrink-0" viewBox="0 0 20 20" fill="currentColor">
                  <path fill-rule="evenodd" d="M5.05 4.05a7 7 0 119.9 9.9L10 18.9l-4.95-4.95a7 7 0 010-9.9zM10 11a2 2 0 100-4 2 2 0 000 4z" clip-rule="evenodd" />
                </svg>
                <div class="text-lg font-medium text-white truncate"><%= favorite.name %></div>
              </div>

              <div class="text-white/60 text-sm">
                <div class="mb-1">Added on <%= new Date(favorite.createdAt).toLocaleDateString('en-US', { 
                  weekday: 'long',
                  year: 'numeric',
                  month: 'long',
                  day: 'numeric'
                }) %></div>
                <div>Coordinates: <%= favorite.latitude.toFixed(4) %>, <%= favorite.longitude.toFixed(4) %></div>
              </div>

              <div class="mt-4 text-sm">
                <span class="glass-button text-sm py-1">
                  View Weather
                  <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 inline ml-1" viewBox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M10.293 3.293a1 1 0 011.414 0l6 6a1 1 0 010 1.414l-6 6a1 1 0 01-1.414-1.414L14.586 11H3a1 1 0 110-2h11.586l-4.293-4.293a1 1 0 010-1.414z" clip-rule="evenodd" />
                  </svg>
                </span>
              </div>
            </a>
          </div>
        <% }); %>
      </div>
    <% } else { %>
      <div class="text-center py-12">
        <div class="text-white/70 text-lg mb-4">No favorite locations yet</div>
        <p class="text-white/60 mb-6">Start by adding locations to your favorites from the dashboard</p>
        <a href="/dashboard" class="glass-button">Go to Dashboard</a>
      </div>
    <% } %>
  </div>
</div>

<script src="/assets/js/theme.js"></script> 
</body>
</html>